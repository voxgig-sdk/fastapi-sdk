<?php
declare(strict_types=1);

// Fastapi SDK utility: feature_hook

class FastapiFeatureHook
{
    public static function call(FastapiContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
